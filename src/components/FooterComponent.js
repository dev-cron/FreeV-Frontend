import React from 'react'
import { Layout } from 'antd';

const { Footer } = Layout;

export const FooterComponent = () => {
  return (
    <Footer style={{ textAlign: 'center' }}>Cool video platform here for sharing videos freely and unrestricted </Footer>
  )
}
